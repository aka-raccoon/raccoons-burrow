apiVersion: gemini.fairwinds.com/v1beta1
kind: SnapshotGroup
metadata:
  name: readarr-backup
  namespace: downloads
spec:
  persistentVolumeClaim:
    claimName: readarr-config
  schedule:
    - every: day
      keep: 3